apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debate.fullname" . }}-test-connection"
  labels:
    {{- include "debate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
