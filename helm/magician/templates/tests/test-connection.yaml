apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "magician.fullname" . }}-test-connection"
  labels:
    {{- include "magician.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "magician.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
