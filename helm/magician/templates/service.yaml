apiVersion: v1
kind: Service
metadata:
  name: {{ include "magician.fullname" . }}
  labels:
    {{- include "magician.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "magician.selectorLabels" . | nindent 4 }}
